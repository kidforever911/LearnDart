void main(List<String> args) {
  // var ls = [1, 2, 3, 4, 5];

  // print(ls);

  // var ws = [];

  // ws..add(1)..add(2)..add(3)..add(4)..add(5);

  // print(ws);

  // var ws = List<int>.filled(3, 0, growable: true);
  // ws[0] = 1;
  // ws[1] = 2;
  // ws[2] = 3;
  // ws.add(4);

  // var ws = List.generate(3, (index) => index + 1);

  // print(ws);

  // var l = [1, 2, 3, 4, 5];

  // print(l.first);
  // print(l.last);
  // print(l.length);
  // print(l.isEmpty);
  // print(l.isNotEmpty);
  // print(l.reversed);
  // print(l.reversed.toList());


  // var ls  = [];
  // ls..add(1)..addAll([2, 3, 4, 5])..insert(0, 6)..insertAll(0, [7, 8, 9]);

  // print(ls);

//   var ls = [1, 2, 3, 4, 5];

//   print(ls.indexOf(3));
//   print(ls.indexWhere((element) {
//     return element > 3;
//   }));

  // var ls = [1, 2, 3, 4, 5];
  // ls.remove(3);
  // print(ls);

  // ls.removeAt(0);
  // print(ls);

  // var ls = [1, 2, 3, 4, 5];
  // ls.fillRange(0, 3, 99);
  // print(ls);

  // print(ls.getRange(0, 3));

  // var ls = [1, 2, 3, 4, 5];
  // ls.shuffle();
  // print(ls);
  // ls.shuffle();
  // print(ls);

  // var ls = [8, 2, 5, 4, 5, 6, 7, 8, 9, 10];

  // ls.sort();

  // print(ls);

  // List<DateTime> dtList = [];
  // dtList.addAll([
  //   DateTime(2021, 5, 15, 9, 30, 15),
  //   DateTime(2021, 5, 12, 9, 30, 16),
  //   DateTime(2021, 5, 16, 9, 30, 17),
  //   DateTime(2021, 5, 13, 9, 30, 18),
  //   DateTime(2021, 5, 13, 9, 30, 19),
  //   DateTime(2021, 5, 18, 9, 30, 20),
  //   DateTime(2021, 5, 13, 9, 30, 21),
  //   DateTime(2021, 5, 13, 9, 30, 22),
  //   DateTime(2021, 5, 13, 9, 30, 23),
  //   DateTime(2021, 5, 13, 9, 30, 24),
  // ]);
  // print(dtList);
  // dtList.sort((a, b) => a.compareTo(b));

  // print(dtList);

  // var ls = [1, 2, 3, 4, 5];
  // ls[0] = 99;
  // print(ls[0]);

  // ls += [6, 7, 8, 9, 10];
  // print(ls);

}